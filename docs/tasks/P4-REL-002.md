# P4-REL-002 - Backup / Restore Device UI

## الهدف

إضافة واجهة تجريبية مستقلة على نفس أصل Studio5 تتيح للمستخدم تنزيل Backup كامل، اختيار Backup من
الجهاز، فحصه، ثم استعادته بعد تأكيد صريح.

## النطاق

- Route مستقلة: `/reliability/`.
- فتح نفس IndexedDB ومخزن المحتوى المستخدمين في Library.
- عرض ملخص البيانات الحالية.
- إنشاء وتنزيل ملف `.studio5-backup.json`.
- منتقي ملف أصلي ظاهر ومتوافق مع MatePad.
- فحص الملف قبل تفعيل الاستعادة.
- عرض Manifest: التاريخ، Schema، عدد الكيانات، عدد الملفات/Ink، والحجم.
- Checkbox تأكيد إلزامي قبل استبدال البيانات.
- Reload بعد الاستعادة حتى تعيد كل الوحدات فتح Repository من البيانات الجديدة.
- لا قفل، لا تشفير، لا Cloud، لا Merge تلقائي.

## الملفات المحجوزة

- `prototype/p3-lecture-capture-web/reliability/**`
- `prototype/p3-lecture-capture-web/scripts/verify-build.mjs`
- `prototype/p3-lecture-capture-web/scripts/typecheck.mjs`
- `prototype/p3-lecture-capture-web/sw.js`
- `prototype/p3-lecture-capture-web/package.json`
- `prototype/p3-lecture-capture-web/tests/reliability-ui.test.mjs`
- `prototype/p3-lecture-capture-web/tests/upload-ui.test.mjs`
- `prototype/p3-lecture-capture-web/tests/worker.test.mjs`
- `docs/tasks/P4-REL-002.md`
- `PROJECT_STATUS.md`
- `docs/TRACEABILITY.md`

## معايير القبول

- [x] تفتح الشاشة من Route مستقلة ولا تعتمد على UI أخرى.
- [x] تعرض عدّاً مفهوماً للبيانات الحالية.
- [x] ينزل Backup باسم مؤرخ وامتداد واضح.
- [x] حقل الاستعادة Native ومرئي.
- [x] ملف تالف لا يفعّل زر الاستعادة.
- [x] Manifest صحيح يظهر قبل الاستعادة.
- [x] لا تعمل الاستعادة قبل Checkbox التأكيد.
- [x] نجاح الاستعادة يطلب Reload ويعيد فتح البيانات.
- [x] Build واختبارات UI وCore وInk ناجحة.
- [ ] تُنشر نسخة Sites خاصة وتنتظر اختبار MatePad.

## بوابة الجهاز

1. أضف PDF وملاحظة، وتأكد من وجود رسم محفوظ.
2. افتح `/reliability/` ونزّل Backup.
3. اختر الملف نفسه من الجهاز.
4. تأكد من ظهور Manifest.
5. فعّل التأكيد واضغط استعادة.
6. أعد فتح Library وInk وتأكد من بقاء PDF والملاحظة والرسم.

## Replace / Retire

الواجهة `Experimental`. يمكن استبدالها أو إخفاؤها من دون تغيير تنسيق Backup أو حذف البيانات؛ العقود
داخل `packages/studio5-core/src/backup.mjs`.

## Rollback

الرجوع إلى `codex/p4-backup-restore-core` يزيل Route فقط ويبقي محرك Backup وبيانات المستخدم.

## نتيجة التحقق قبل النشر

- Reliability + Phase 3 UI: `17/17 PASS`.
- Core: `95/95 PASS`.
- Ink regression: `15/15 PASS`.
- Lint وType Check وBuild: PASS.
