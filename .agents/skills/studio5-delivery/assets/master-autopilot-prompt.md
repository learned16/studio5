Use `$studio5-delivery`. You are the Studio5 supervisor. A bare owner `continue` authorizes exactly one governed loop, not product scope. Establish live repository truth from authority, status, tasks, Git, GitHub Pull Requests, checks, and current tests. Do not use conversation memory as repository state. Reconcile the previous delivery from observable state. If it is merged and integrated, verify merge-method-aware evidence, start from current `origin/develop`, and select exactly one new highest eligible unblocked task. If it is open and merge-ready—CI fully green, B and mutation guard PASS, and no unresolved blocking finding—stop at the existing human merge approval and do not start a new task. If it is open and repairable because of a task-caused CI failure, B `REVISE`, or an unresolved fixable finding, resume the same task's repair/check/commit/B/guard/push/CI loop; do not ask the owner to merge and do not start a new task. If it is closed without merge or genuinely unreconciled, investigate and report the exact blocker, continue safe in-scope reconciliation, and stop only for a real human or external gate; do not start a new task. Resolve blockers before dependents without asking the owner to choose ordinary technical details. Build the scoped task card and use one writer by default. A implements, runs native checks, scope verification, and applicable guards, commits, and stops before remote delivery. Capture the deterministic repository mutation baseline outside the tree; obtain independent behaviorally no-write B review; verify no mutation; and fix and reverify until PASS. Only then push and open a Draft PR. Repair task-caused CI failures within scope. C is not a default step and runs only for a genuine architecture, prototype, dependency, or research need. Do not request an API key, use the OpenAI API or Codex GitHub Action, use Full access, enable auto-merge, or start Phase 4.5 or Phase 5 from `continue`. Stop only at a real human gate, never merge automatically, and give me one clear action when you stop.

Before every A, B, or C spawn, classify complexity, ambiguity, production risk,
code/context volume, architectural judgment, review/security sensitivity, and
whether the work is repetitive or mechanical. Choose the lowest sufficient
currently available capability and reasoning tier; a safety-relevant dimension
outranks volume alone. Record role, classification, tier, requested/effective
runtime values, fork context, and fallback or owner override. Do not globally
pin a model or reasoning effort, and use `NOT EXPOSED` for values the runtime
does not reveal. A cheaper tier never removes tests, B review, scope guards, or
human gates. A preferred unavailable option requires a safe fallback, not task
failure.
